---
title: Next.js
description: Use FormWire with a Next.js application.
sidebar:
  order: 4
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

FormWire works in both the browser and on the server, so you can submit from a Client Component or from a Server Action.

<Tabs>
<TabItem label="Client Component" icon="seti:react">

```jsx title="app/contact/contact-form.js"
"use client";
import { useState } from "react";

const ENDPOINT = "https://api.formwire.app/v1/submit/YOUR_FORM_SECRET";

export function ContactForm() {
  const [status, setStatus] = useState("idle");

  async function handleSubmit(event) {
    event.preventDefault();
    setStatus("submitting");
    const payload = Object.fromEntries(new FormData(event.target).entries());
    const res = await fetch(ENDPOINT, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload),
    });
    setStatus(res.ok ? "success" : "error");
  }

  return (
    <form onSubmit={handleSubmit}>
      <input name="email" type="email" required />
      <textarea name="message" />
      <input name="_fw_hp" tabIndex={-1} autoComplete="off" hidden />
      <button disabled={status === "submitting"}>Send</button>
    </form>
  );
}
```

</TabItem>
<TabItem label="Server Action" icon="forward-slash">

Submitting from a Server Action keeps your form secret off the client entirely:

```jsx title="app/contact/actions.js"
"use server";

export async function submitContact(formData) {
  const payload = Object.fromEntries(formData.entries());
  const res = await fetch(
    `https://api.formwire.app/v1/submit/${process.env.FORMWIRE_SECRET}`,
    {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload),
    }
  );
  if (!res.ok) throw new Error("Submission failed");
}
```

```jsx title="app/contact/page.js"
import { submitContact } from "./actions";

export default function ContactPage() {
  return (
    <form action={submitContact}>
      <input name="email" type="email" required />
      <textarea name="message" />
      <button>Send</button>
    </form>
  );
}
```

:::caution
Store the secret in an environment variable (e.g. `FORMWIRE_SECRET`) rather than hardcoding it.
:::

</TabItem>
</Tabs>

## Next steps

- [Spam protection](/guides/spam-protection)
- [Schema validation](/guides/schema-validation)
